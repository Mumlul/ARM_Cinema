<Page x:Class="trizbd.Pages.Dashboard"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      mc:Ignorable="d"
      Title="Главная">

    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
        <Grid Margin="60,50,60,50">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0" Margin="0,0,0,50">
                <TextBlock Text="Главная" Style="{StaticResource PageTitle}"/>
                <TextBlock Text="ОБЗОР СЕГОДНЯ" Style="{StaticResource PageSubtitle}" Margin="0,10,0,0"/>
            </StackPanel>

            <UniformGrid Grid.Row="1" Columns="3" Margin="0,0,0,40">
                <Border Style="{StaticResource CardStyle}" Margin="0,0,25,0">
                    <StackPanel>
                        <TextBlock Text="СЕГОДНЯ ПРОДАНО" FontFamily="{StaticResource FontMono}" FontSize="10" Foreground="{DynamicResource TextMutedBrush}"/>
                        <TextBlock x:Name="SoldTodayTb" Text="—" FontSize="48" FontWeight="Light" Margin="0,10,0,0">
                            <TextBlock.Foreground>
                                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                    <GradientStop Color="{StaticResource PrimaryColor}" Offset="0"/>
                                    <GradientStop Color="{StaticResource SecondaryColor}" Offset="1"/>
                                </LinearGradientBrush>
                            </TextBlock.Foreground>
                        </TextBlock>
                        <TextBlock x:Name="SoldTodayHintTb" Text="" FontFamily="{StaticResource FontMono}" FontSize="11" Foreground="{StaticResource SuccessBrush}" Margin="0,8,0,0"/>
                    </StackPanel>
                </Border>

                <Border Style="{StaticResource CardStyle}" Margin="0,0,25,0">
                    <StackPanel>
                        <TextBlock Text="ВЫРУЧКА" FontFamily="{StaticResource FontMono}" FontSize="10" Foreground="{DynamicResource TextMutedBrush}"/>
                        <TextBlock x:Name="RevenueTodayTb" Text="—" FontSize="48" FontWeight="Light" Margin="0,10,0,0">
                            <TextBlock.Foreground>
                                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                    <GradientStop Color="{StaticResource PrimaryColor}" Offset="0"/>
                                    <GradientStop Color="{StaticResource SecondaryColor}" Offset="1"/>
                                </LinearGradientBrush>
                            </TextBlock.Foreground>
                        </TextBlock>
                        <TextBlock x:Name="RevenueTodayHintTb" Text="" FontFamily="{StaticResource FontMono}" FontSize="11" Foreground="{StaticResource SuccessBrush}" Margin="0,8,0,0"/>
                    </StackPanel>
                </Border>

                <Border Style="{StaticResource CardStyle}">
                    <StackPanel>
                        <TextBlock Text="АКТИВНЫЕ СЕАНСЫ" FontFamily="{StaticResource FontMono}" FontSize="10" Foreground="{DynamicResource TextMutedBrush}"/>
                        <TextBlock x:Name="ActiveSessionsTb" Text="—" FontSize="48" FontWeight="Light" Margin="0,10,0,0">
                            <TextBlock.Foreground>
                                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                    <GradientStop Color="{StaticResource PrimaryColor}" Offset="0"/>
                                    <GradientStop Color="{StaticResource SecondaryColor}" Offset="1"/>
                                </LinearGradientBrush>
                            </TextBlock.Foreground>
                        </TextBlock>
                        <TextBlock Text="на сегодня" FontFamily="{StaticResource FontMono}" FontSize="11" Foreground="{DynamicResource TextMutedBrush}" Margin="0,8,0,0"/>
                    </StackPanel>
                </Border>
            </UniformGrid>

            <Border Grid.Row="2" Style="{StaticResource CardStyle}">
                <StackPanel>
                    <TextBlock Text="Последние продажи" FontSize="20" FontWeight="SemiBold" Margin="0,0,0,20"/>
                    <DataGrid x:Name="RecentSalesGrid" Style="{StaticResource DataGridDark}" AutoGenerateColumns="False" Height="260">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Время" Binding="{Binding SaleTime}" Width="120"/>
                            <DataGridTextColumn Header="Фильм" Binding="{Binding Movie}" Width="*"/>
                            <DataGridTextColumn Header="Зал" Binding="{Binding Hall}" Width="120"/>
                            <DataGridTextColumn Header="Место" Binding="{Binding Seat}" Width="110"/>
                            <DataGridTextColumn Header="Цена" Binding="{Binding Price}" Width="120"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </StackPanel>
            </Border>

        </Grid>
    </ScrollViewer>
</Page>
