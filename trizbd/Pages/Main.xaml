<Page x:Class="trizbd.Pages.Main"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      mc:Ignorable="d"
      Title="Главное окно">

    <Grid Background="{DynamicResource AppBackgroundBrush}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="240"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0"
                Background="{DynamicResource SidebarBackgroundBrush}"
                BorderBrush="{DynamicResource DividerBrush}"
                BorderThickness="0,0,1,0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Text="АРМ ОПЕРАТОРА"
                           FontFamily="{StaticResource FontMono}"
                           FontSize="11"
                           FontWeight="Bold"
                           Margin="30,40,30,0">
                    <TextBlock.Foreground>
                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                            <GradientStop Color="{StaticResource PrimaryColor}" Offset="0"/>
                            <GradientStop Color="{StaticResource SecondaryColor}" Offset="1"/>
                        </LinearGradientBrush>
                    </TextBlock.Foreground>
                </TextBlock>

                <StackPanel Grid.Row="1" Margin="0,60,0,0">
                    <Button x:Name="NavDashboard" Content="Главная" Style="{StaticResource NavButtonStyle}" Tag="Dashboard" Click="Nav_Click"/>
                    <Button x:Name="NavSchedule" Content="Расписание" Style="{StaticResource NavButtonStyle}" Tag="Schedule" Click="Nav_Click"/>
                    <Button x:Name="NavTickets" Content="Продажа билетов" Style="{StaticResource NavButtonStyle}" Tag="Tickets" Click="Nav_Click"/>
                    <Button x:Name="NavMovies" Content="Фильмы" Style="{StaticResource NavButtonStyle}" Tag="Movies" Click="Nav_Click"/>
                    <Button x:Name="NavReports" Content="Отчёты" Style="{StaticResource NavButtonStyle}" Tag="Reports" Click="Nav_Click"/>
                    <Button x:Name="NavAdmin" Content="Администрирование" Style="{StaticResource NavButtonStyle}" Tag="Admin" Click="Nav_Click"/>
                </StackPanel>

                <StackPanel Grid.Row="2" Margin="30,0,30,25">
                    <TextBlock x:Name="UserNameTb" FontSize="14" Margin="0,0,0,6"/>
                    <TextBlock x:Name="UserRoleTb" FontFamily="{StaticResource FontMono}" FontSize="11" Foreground="{DynamicResource TextMutedBrush}"/>
                    <Button x:Name="ThemeToggleBtn" Content="Светлая тема" Style="{StaticResource OutlineButtonStyle}" Margin="0,12,0,0" Click="ThemeToggle_Click"/>
                    <Button Content="Выйти" Style="{StaticResource OutlineButtonStyle}" Margin="0,20,0,0" Click="Logout_Click"/>
                </StackPanel>
            </Grid>
        </Border>

        <Grid Grid.Column="1">
            <Frame x:Name="ContentFrame" NavigationUIVisibility="Hidden"/>
        </Grid>
    </Grid>
</Page>
