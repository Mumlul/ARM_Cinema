<Page x:Class="trizbd.Pages.Schedule"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      mc:Ignorable="d"
      Title="Расписание">

    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
        <Grid Margin="60,50,60,50">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0" Margin="0,0,0,20">
                <TextBlock Text="Расписание" Style="{StaticResource PageTitle}"/>
                <TextBlock Text="СЕАНСЫ НА ДАТУ" Style="{StaticResource PageSubtitle}" Margin="0,10,0,0"/>
            </StackPanel>

            <Border Grid.Row="1" Style="{StaticResource CardStyle}" Margin="0,0,0,25" Padding="22">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <TextBlock Text="Дата" Style="{StaticResource MonoText}" FontSize="11" Foreground="{DynamicResource TextMutedBrush}" VerticalAlignment="Center"/>
                        <DatePicker x:Name="DateDp" Style="{StaticResource FormDatePicker}" Margin="12,0,0,0" SelectedDateChanged="DateDp_SelectedDateChanged"/>
                    </StackPanel>

                    <Button Grid.Column="1" Content="Обновить" Style="{StaticResource OutlineButtonStyle}" Margin="20,0,0,0" Click="RefreshBtn_Click"/>
                </Grid>
            </Border>

            <StackPanel Grid.Row="2">
                <TextBlock x:Name="EmptyTb" Text="Нет сеансов на выбранную дату" Visibility="Collapsed" Style="{StaticResource MonoText}" FontSize="12" Foreground="{DynamicResource TextMutedBrush}" Margin="0,10,0,0"/>

                <ItemsControl x:Name="ScheduleIc">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Background="#4D251C3D" BorderBrush="#268B5CF6" BorderThickness="1" CornerRadius="2" Padding="22" Margin="0,0,0,16">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="110"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="120"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>

                                    <TextBlock Text="{Binding Time}" FontFamily="{StaticResource FontMono}" FontSize="26" Foreground="{StaticResource SecondaryBrush}" FontWeight="Bold" VerticalAlignment="Center"/>

                                    <StackPanel Grid.Column="1" Margin="20,0,0,0">
                                        <TextBlock Text="{Binding Movie}" FontSize="18" FontWeight="SemiBold"/>
                                        <TextBlock Text="{Binding Hall}" Style="{StaticResource MonoText}" FontSize="10" Foreground="{DynamicResource TextMutedBrush}" Margin="0,6,0,0"/>
                                        <TextBlock Text="Мест занято / всего: " Style="{StaticResource MonoText}" FontSize="10" Foreground="{DynamicResource TextMutedBrush}" Margin="0,8,0,0"/>
                                        <TextBlock Text="{Binding SeatsInfo}" Style="{StaticResource MonoText}" FontSize="11" Foreground="{DynamicResource TextSecondaryBrush}"/>
                                    </StackPanel>

                                    <StackPanel Grid.Column="2" VerticalAlignment="Center">
                                        <TextBlock Text="{Binding Price}" Style="{StaticResource MonoText}" FontSize="12" Foreground="{DynamicResource TextPrimaryBrush}"/>
                                        <Border Margin="0,10,0,0" Padding="8,6" CornerRadius="2">
                                            <Border.Style>
                                                <Style TargetType="Border">
                                                    <Setter Property="Background" Value="#3334D399"/>
                                                    <Setter Property="BorderBrush" Value="{StaticResource SuccessBrush}"/>
                                                    <Setter Property="BorderThickness" Value="1"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding IsSoldOut}" Value="True">
                                                            <Setter Property="Background" Value="#33FBBF24"/>
                                                            <Setter Property="BorderBrush" Value="{StaticResource WarningBrush}"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </Border.Style>
                                            <TextBlock Text="{Binding StatusText}" FontFamily="{StaticResource FontMono}" FontSize="10" Foreground="{DynamicResource TextPrimaryBrush}"/>
                                        </Border>
                                    </StackPanel>

                                    <StackPanel Grid.Column="3" VerticalAlignment="Center" Margin="20,0,0,0">
                                        <Button Content="Продажа" Style="{StaticResource PrimaryButtonStyle}" Padding="14,10" Tag="{Binding}" Click="SellFromScheduleBtn_Click"/>
                                    </StackPanel>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </StackPanel>
        </Grid>
    </ScrollViewer>
</Page>
