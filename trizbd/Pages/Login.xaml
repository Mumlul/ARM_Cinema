<Page x:Class="trizbd.Pages.Login"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      mc:Ignorable="d"
      Title="Авторизация">

    <Grid Background="{DynamicResource AppBackgroundBrush}">

        <Ellipse Width="600" Height="600" Opacity="0.15" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="0,-200,-200,0">
            <Ellipse.Fill>
                <RadialGradientBrush>
                    <GradientStop Color="{StaticResource PrimaryColor}" Offset="0"/>
                    <GradientStop Color="Transparent" Offset="0.7"/>
                </RadialGradientBrush>
            </Ellipse.Fill>
            <Ellipse.RenderTransform>
                <TranslateTransform x:Name="Blob1"/>
            </Ellipse.RenderTransform>
        </Ellipse>

        <Ellipse Width="400" Height="400" Opacity="0.10" HorizontalAlignment="Left" VerticalAlignment="Bottom" Margin="-100,0,0,-100">
            <Ellipse.Fill>
                <RadialGradientBrush>
                    <GradientStop Color="{StaticResource SecondaryColor}" Offset="0"/>
                    <GradientStop Color="Transparent" Offset="0.7"/>
                </RadialGradientBrush>
            </Ellipse.Fill>
            <Ellipse.RenderTransform>
                <TranslateTransform x:Name="Blob2"/>
            </Ellipse.RenderTransform>
        </Ellipse>

        <Border Width="420"
                Padding="60,55"
                Background="{DynamicResource CardBackgroundBrush}"
                BorderBrush="{DynamicResource CardBorderBrush}"
                BorderThickness="1"
                CornerRadius="4"
                HorizontalAlignment="Center"
                VerticalAlignment="Center">
            <Border.Effect>
                <DropShadowEffect BlurRadius="40" ShadowDepth="0" Opacity="0.45" Color="#000000"/>
            </Border.Effect>

            <StackPanel>
                <TextBlock Text="АРМ ОПЕРАТОРА КИНОТЕАТРА"
                           FontFamily="{StaticResource FontMono}"
                           FontSize="14"
                           FontWeight="Bold"
                           TextAlignment="Center"
                           Margin="0,0,0,50">
                    <TextBlock.Foreground>
                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                            <GradientStop Color="{StaticResource PrimaryColor}" Offset="0"/>
                            <GradientStop Color="{StaticResource SecondaryColor}" Offset="1"/>
                        </LinearGradientBrush>
                    </TextBlock.Foreground>
                </TextBlock>

                <StackPanel Margin="0,0,0,26">
                    <TextBlock Text="ЛОГИН" FontFamily="{StaticResource FontMono}" FontSize="11" Foreground="{DynamicResource TextMutedBrush}"/>
                    <TextBox x:Name="UsernameTb" Style="{StaticResource UnderlinedTextBox}"/>
                </StackPanel>

                <StackPanel Margin="0,0,0,10">
                    <TextBlock Text="ПАРОЛЬ" FontFamily="{StaticResource FontMono}" FontSize="11" Foreground="{DynamicResource TextMutedBrush}"/>
                    <PasswordBox x:Name="PasswordPb" Background="Transparent" BorderThickness="0" Foreground="{DynamicResource TextPrimaryBrush}" FontSize="18" Padding="0,8,0,8"/>
                    <Border Height="1" Background="{DynamicResource ControlBorderBrush}" Margin="0,-2,0,0"/>
                </StackPanel>

                <TextBlock x:Name="ErrorTb" Visibility="Collapsed" Foreground="#FFFBBF24" FontFamily="{StaticResource FontMono}" FontSize="11" Margin="0,10,0,0"/>

                <Button Content="Войти в систему"
                        Style="{StaticResource PrimaryButtonStyle}"
                        Margin="0,22,0,0"
                        Click="LoginButton_Click"/>
            </StackPanel>
        </Border>

        <Grid.Triggers>
            <EventTrigger RoutedEvent="FrameworkElement.Loaded">
                <BeginStoryboard>
                    <Storyboard>
                        <DoubleAnimation Storyboard.TargetName="Blob1" Storyboard.TargetProperty="Y" From="0" To="-30" Duration="0:0:4" AutoReverse="True" RepeatBehavior="Forever"/>
                        <DoubleAnimation Storyboard.TargetName="Blob2" Storyboard.TargetProperty="Y" From="0" To="30" Duration="0:0:3" AutoReverse="True" RepeatBehavior="Forever"/>
                    </Storyboard>
                </BeginStoryboard>
            </EventTrigger>
        </Grid.Triggers>

    </Grid>
</Page>
