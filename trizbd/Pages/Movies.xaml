<Page x:Class="trizbd.Pages.Movies"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      mc:Ignorable="d"
      Title="Фильмы">

    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
        <Grid Margin="60,50,60,50">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0" Margin="0,0,0,20">
                <TextBlock Text="Фильмы" Style="{StaticResource PageTitle}"/>
                <TextBlock Text="КАТАЛОГ • ПОИСК • ДЕТАЛИ" Style="{StaticResource PageSubtitle}" Margin="0,10,0,0"/>
            </StackPanel>

            <Border Grid.Row="1" Style="{StaticResource CardStyle}" Margin="0,0,0,25" Padding="22">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <TextBox x:Name="SearchTb" Style="{StaticResource FormTextBox}" TextChanged="SearchTb_TextChanged"
                             VerticalAlignment="Center" Margin="0,0,20,0"/>

                    <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,15,0">
                        <TextBlock Text="Найдено:" Style="{StaticResource MonoText}" FontSize="11" Foreground="{DynamicResource TextMutedBrush}" VerticalAlignment="Center"/>
                        <TextBlock x:Name="CountTb" Text="0" Style="{StaticResource MonoText}" FontSize="11" Margin="10,0,0,0" VerticalAlignment="Center"/>
                    </StackPanel>

                    <!-- Кнопки администратора -->
                    <Button x:Name="CreateSessionBtn"
                            Grid.Column="2"
                            Content="Создать сеанс"
                            Style="{StaticResource OutlineButtonStyle}"
                            Padding="14,10"
                            Visibility="Collapsed"
                            Click="CreateSessionBtn_Click"
                            Margin="0,0,10,0"/>

                    <Button x:Name="ImportMoviesBtn"
                            Grid.Column="3"
                            Content="Импорт фильмов"
                            Style="{StaticResource OutlineButtonStyle}"
                            Padding="14,10"
                            Visibility="Collapsed"
                            Click="ImportMoviesBtn_Click"/>
                </Grid>
            </Border>

            <StackPanel Grid.Row="2">
                <TextBlock x:Name="EmptyTb" Text="Ничего не найдено" Visibility="Collapsed" Style="{StaticResource MonoText}"
                           FontSize="12" Foreground="{DynamicResource TextMutedBrush}" Margin="0,10,0,0"/>

                <ItemsControl x:Name="MoviesIc">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel IsItemsHost="True"/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>

                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Button Tag="{Binding}" Click="OpenMovieBtn_Click" Background="Transparent" BorderThickness="0" Padding="0" Margin="0,0,30,30" Cursor="Hand">
                                <Border Width="220" Style="{StaticResource CardStyle}" Padding="18">
                                    <StackPanel>
                                        <Border Height="160" CornerRadius="2" BorderBrush="#338B5CF6" BorderThickness="1" Margin="0,0,0,14">
                                            <Border.Background>
                                                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                                    <GradientStop Color="{StaticResource PrimaryColor}" Offset="0"/>
                                                    <GradientStop Color="{StaticResource SecondaryColor}" Offset="1"/>
                                                </LinearGradientBrush>
                                            </Border.Background>
                                            <TextBlock Text="🎬" FontSize="54" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>

                                        <TextBlock Text="{Binding Title}" FontSize="16" FontWeight="SemiBold"/>
                                        <TextBlock Text="{Binding Meta}" Style="{StaticResource MonoText}" FontSize="10" Foreground="{DynamicResource TextMutedBrush}" Margin="0,6,0,0"/>
                                        <TextBlock Text="{Binding Genres}" Style="{StaticResource MonoText}" FontSize="10" Foreground="{DynamicResource TextSecondaryBrush}" Margin="0,10,0,0" TextWrapping="Wrap"/>
                                    </StackPanel>
                                </Border>
                            </Button>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </StackPanel>
        </Grid>
    </ScrollViewer>
</Page>
