<Page x:Class="trizbd.Pages.Movie_Details"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      mc:Ignorable="d"
      Title="Детали фильма">

    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
        <Grid Margin="60,50,60,50">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0" Margin="0,0,0,30">
                <StackPanel Orientation="Horizontal">
                    <Button Content="← Назад" Style="{StaticResource OutlineButtonStyle}" Click="BackBtn_Click"/>
                </StackPanel>
            </StackPanel>

            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="320"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Border Grid.Column="0" Height="460" Style="{StaticResource CardStyle}" Padding="0">
                    <Border.Background>
                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                            <GradientStop Color="{StaticResource PrimaryColor}" Offset="0"/>
                            <GradientStop Color="{StaticResource SecondaryColor}" Offset="1"/>
                        </LinearGradientBrush>
                    </Border.Background>
                    <Viewbox>
                        <TextBlock Text="🎬" FontSize="120"/>
                    </Viewbox>
                </Border>

                <StackPanel Grid.Column="1" Margin="50,0,0,0">
                    <TextBlock x:Name="TitleTb" Text="—" FontSize="36" FontWeight="Light"/>
                    <TextBlock x:Name="MetaTb" Text="—" Style="{StaticResource MonoText}" FontSize="11" Foreground="{DynamicResource TextMutedBrush}" Margin="0,10,0,0"/>

                    <Border Height="1" Background="#268B5CF6" Margin="0,25,0,25"/>

                    <TextBlock Text="Жанры" Style="{StaticResource MonoText}" FontSize="10" Foreground="{DynamicResource TextMutedBrush}"/>
                    <TextBlock x:Name="GenresTb" Text="—" FontSize="16" Foreground="{DynamicResource TextSecondaryBrush}" Margin="0,8,0,0" TextWrapping="Wrap"/>

                    <TextBlock Text="Описание" Style="{StaticResource MonoText}" FontSize="10" Foreground="{DynamicResource TextMutedBrush}" Margin="0,25,0,0"/>
                    <TextBlock x:Name="DescriptionTb" Text="—" FontSize="16" Foreground="{DynamicResource TextSecondaryBrush}" Margin="0,8,0,0" TextWrapping="Wrap"/>

                    <Border Style="{StaticResource CardStyle}" Margin="0,35,0,0">
                        <StackPanel>
                            <TextBlock Text="Ближайшие сеансы (7 дней)" FontSize="20" FontWeight="SemiBold" Margin="0,0,0,18"/>
                            <TextBlock x:Name="SessionsEmptyTb" Text="Нет сеансов" Visibility="Collapsed" Style="{StaticResource MonoText}" FontSize="12" Foreground="{DynamicResource TextMutedBrush}"/>

                            <ItemsControl x:Name="SessionsIc">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Border Background="#3D251C3D" BorderBrush="#268B5CF6" BorderThickness="1" CornerRadius="2" Padding="18" Margin="0,0,0,12">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="Auto"/>
                                                </Grid.ColumnDefinitions>
                                                <StackPanel>
                                                    <TextBlock Text="{Binding Time}" FontFamily="{StaticResource FontMono}" FontSize="14" Foreground="{DynamicResource TextPrimaryBrush}"/>
                                                    <TextBlock Text="{Binding Hall}" Style="{StaticResource MonoText}" FontSize="10" Foreground="{DynamicResource TextMutedBrush}" Margin="0,6,0,0"/>
                                                    <TextBlock Text="{Binding Price}" Style="{StaticResource MonoText}" FontSize="11" Foreground="{DynamicResource TextSecondaryBrush}" Margin="0,6,0,0"/>
                                                </StackPanel>

                                                <Button Grid.Column="1" Content="Продажа" Style="{StaticResource PrimaryButtonStyle}" Padding="12,8" Tag="{Binding}" Click="SellFromMovieBtn_Click"/>
                                            </Grid>
                                        </Border>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </Grid>
        </Grid>
    </ScrollViewer>
</Page>
